{
  "_comment": "Fields allowed to contain non-deterministic values in goldens. All golden files must use deterministic sentinel values instead of runtime values. The epoch sentinel '1970-01-01T00:00:00Z' is exempt from datetime flagging by the verifier without needing an allowlist entry."
}
